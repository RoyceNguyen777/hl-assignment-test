import styled from "styled-components";
import { colors } from "../../../assets";

const StyleParaph = styled.p`
  text-align: center;
  color: ${colors.dark_text};
  line-height: 1.5rem;
`;
export { StyleParaph };
